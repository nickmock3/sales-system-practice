# 028 フロントエンドの税率マスタ画面を作成する

## 目的

税率マスタをブラウザから確認・登録できるようにし、売上日に応じて適用税率を選ぶ業務ルールを画面から学習できるようにする。

## 前提タスク

- `024-redesign-api-contracts-around-aggregates.md`
- `025-refactor-master-apis-around-aggregates.md`
- `026-refactor-sales-api-contracts.md`
- `027-update-existing-master-frontends-for-new-apis.md`

## 対象範囲

- Next.js フロントエンドに税率マスタ画面を追加する。
- 税率一覧を表示する。
- 税率の新規登録フォームを作成する。
- 指定日時点で適用される税率を確認できるようにする。
- 税率 API 呼び出し処理と TypeScript 型を整理する。
- ローディング、入力エラー、API エラーを画面上で確認できるようにする。

## 前提仕様

- 税率は商品マスタとは別に管理する。
- 商品履歴の税区分と売上日から、適用する税率を決定する。
- 適用する税率は、売上日以前で一番新しい税率とする。
- 得意先別商品単価は単価決定に使い、税率決定には使わない。
- 同じ税率値でも、税区分が異なる場合は別の適用情報として扱う。
- 税率履歴行 ID は通常画面へ表示しない。
- バックエンド API はダミー認証を使うため、フロントエンドから必要なヘッダーを付与する。

## 実装方針

- 商品マスタ画面または得意先マスタ画面で作成した API クライアントや UI パターンがあれば、それに合わせる。
- 一覧、登録フォーム、指定日時点の確認を同じ画面内で扱える構成にする。
- 一覧では税区分コード、税区分名、会計分類、税率、適用開始日を確認できるようにする。
- 画面と API クライアントはタスク 024、025 で確定・実装した契約を使用する。
- API のレスポンス型は TypeScript で定義し、画面側に ad hoc な `any` を広げない。

## 確認項目

- `bun lint` またはプロジェクトで定義されているフロントエンド検証コマンドが成功すること。
- 税率を新規登録できること。
- 税率一覧が登録後の内容を反映すること。
- 指定日時点の確認で、対象日以前で一番新しい税率を確認できること。
- 同じ税率値でも税区分名と会計分類を区別して表示できること。
- API エラー時に画面がクラッシュせず、ユーザーに原因が分かる表示になること。

## 完了時に追記すること

- 実装した画面・コンポーネントの概要
- 追加・変更した API クライアント処理
- 実行した確認コマンドと結果
- 残した制約や次タスク候補

## 実装内容

- `/tax-rates` に税率マスタ画面を追加した。
- 本日時点の税率一覧、選択中の税区分詳細、変更履歴、指定日時点の税率参照を同一画面で確認できるようにした。
- 税率の初回登録と、選択した税区分への税率変更フォームを追加した。
- 税区分コード、税区分名、会計分類、税率、適用開始日を表示し、同じ税率値でも軽減税率と旧標準税率を区別できるようにした。
- 税率は画面上で百分率として入力・表示し、API には小数税率へ変換して送信する。
- 非課税と免税は 0% 固定、課税対象は 0% より大きい値、百分率は小数2桁までかつ 999.99% 以下として入力検証する。
- ローディング、入力エラー、API エラー、認証・認可・通信エラーを既存マスタ画面と同じ表示パターンで扱う。
- 税区分切替時に古い変更履歴や指定日参照の応答が表示を上書きしないよう、リクエスト識別子と選択状態を使って応答を検証する。
- `TaxRateId` などの内部履歴 ID は画面用 TypeScript 型と DOM に含めていない。

## API クライアント

- `GET /api/tax-rates?asOf=` に対応する税率一覧取得処理を追加した。
- `GET /api/tax-rates/{taxCategory}?asOf=` に対応する指定日時点取得処理を追加した。
- `GET /api/tax-rates/{taxCategory}/changes` に対応する変更履歴取得処理を追加した。
- `POST /api/tax-rates/{taxCategory}/changes` を初回登録と税率変更で共通利用する処理を追加した。
- API レスポンスは Zod で検証し、税区分と会計分類を定義済みコードへ制限した。
- API の `Rate` バリデーションエラーを画面の `ratePercent` フィールドへ対応付けた。

## 確認結果

- `cd frontend && bun lint`: 成功
- `cd frontend && bun run test`: 成功（9ファイル、35テスト）
- `cd frontend && bun run build`: 成功
- `cd frontend && bun run test:e2e e2e/tax-rates.spec.ts`: 成功（7テスト）
- ブラウザで `/tax-rates` を開き、主要領域のレイアウトとバックエンド API 停止時のエラー表示を確認した。

## 残した制約・次タスク候補

- Playwright は API モックを使った画面 E2E であり、実バックエンドとの主要正常系結合確認は別途実施対象とする。
- 税区分定義は商品画面と税率画面にそれぞれ存在するため、重複が増える場合は共通モジュール化を検討する。
