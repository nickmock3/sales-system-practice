# AI Development Rules

このリポジトリは販売管理システムの実装練習用子リポジトリです。
AI は以下のルールに従って、ユーザーと共同で開発を進めます。

## 開発の進め方

- 仕様は `specs/` に Markdown で作成する。
- 未完了タスクは `tasks/open/` に Markdown で作成する。
- 完了したタスクは `tasks/done/` に移動する。
- タスクファイル名は `001-create-product-master-api.md` のように、先頭に3桁の連番を付ける。
- タスクの連番は `tasks/open/` と `tasks/done/` を合わせて重複しないように採番する。
- タスクが完了したら、実装内容と確認結果をタスクファイルに追記してから `tasks/done/` に移動する。
- 重要な設計判断は、コードだけで表現せず、関連する仕様またはタスクに理由を残す。

## 技術方針

- バックエンドは C# / .NET / ASP.NET Core Web API を使う。
- ORM は Entity Framework Core を使う。
- 本番想定 DB は Oracle Database とし、Oracle 上で動くことを要件に含める。
- 軽量なローカル開発や学習用途では SQLite ファイルDBを使ってよい。
- テストでは SQLite in-memory を使ってよいが、SQLite の成功だけで Oracle 互換を保証したものとは扱わない。
- Oracle 対応では EF Core の Oracle provider を使い、DB 固有 SQL は必要最小限にする。
- マイグレーション、制約、インデックス、日付比較、金額の精度、ページング、履歴取得クエリは Oracle 上でも確認する。
- バックエンドのテストは xUnit を使う。
- バックエンドの各テストには、そのテストで何を確認するかを日本語の一行コメントで記載する。
- フロントエンドは Next.js、TypeScript、Bun を使う。

## 業務ルール

- マスタの変更は既存行の更新ではなく、履歴レコードの追加で表現する。
- 履歴テーブルは原則 `ValidFrom` のみを持ち、`ValidTo` は持たない。
- 適用する履歴は、対象日以前で一番新しい履歴とする。
- 売上は過去の取引記録として扱い、登録時点で採用したマスタ履歴 ID と金額情報を保存する。
- 税率は商品マスタとは別に管理し、売上日以前で一番新しい税率を適用する。

## コミット方針

- コミットはユーザーが依頼した場合に行う。
- コミットメッセージは `<type>: <変更内容>` の形式にする。
- `type` は `docs`, `feat`, `fix`, `test`, `refactor`, `chore`, `style` を使う。
- 変更内容は日本語で、何を変えたかが分かる短い文にする。
- 仕様、設計メモ、タスク文書、README の変更は `docs` を使う。
